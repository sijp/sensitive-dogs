{
  "name": "sensitive-dogs",
  "version": "1.0.0",
  "description": "Sensitive dogs community index",
  "main": "index.ts",
  "repository": "https://github.com/sijp/sensitive-dogs.git",
  "author": "Shlomi Israely <sijproject@gmail.com>",
  "license": "GPLv3",
  "private": true,
  "scripts": {
    "dev": "yarn --cwd src/core-packages/dev-server start",
    "build": "yarn --cwd src/core-packages/builder start",
    "clean-dist": "rm -rf dist",
    "prod": "yarn clean-dist && yarn build && yarn serve dist"
  },
  "workspaces": [
    "src/**/*"
  ],
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
